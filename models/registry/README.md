# Model Registry

This directory is reserved for versioned trained retention-model artifacts and metadata.
A model is only registered after training on a labeled dataset. The application does not
claim that a model is trained when no labeled dataset is available.
